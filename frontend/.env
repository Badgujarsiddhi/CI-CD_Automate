# Frontend Environment Variables
# Backend API URL (where FastAPI is running)
VITE_API_BASE_URL=http://localhost:8000

# Note: The frontend doesn't directly connect to Ollama.
# It communicates with the backend API, which handles Ollama integration.
# Ollama configuration is done in the backend .env file.
